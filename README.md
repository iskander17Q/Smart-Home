# SmartHome Dashboard

Современное desktop-приложение для управления умным домом с красивым GUI на PySide6 (Qt).

## Возможности

- 📊 **Dashboard** - дашборд с карточками комнат, показателями датчиков и быстрыми действиями
- 🏠 **Комнаты** - управление комнатами и устройствами в них
- 🔌 **Устройства** - добавление, редактирование и управление устройствами (датчики и актуаторы)
- ⚙️ **Автоматизация** - создание правил if-then для автоматического управления
- 📋 **Логи** - просмотр и экспорт событий системы
- ⚙️ **Настройки** - настройка режима работы (Local Simulation / MQTT)

## Установка

### Требования

- Python 3.11 или выше
- Windows, macOS или Linux

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone https://github.com/iskander17Q/Smart-Home.git
cd Smart-Home
```

2. Создайте виртуальное окружение (рекомендуется):
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:

**Windows:**
```bash
venv\Scripts\activate
```

**macOS/Linux:**
```bash
source venv/bin/activate
```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Запуск

```bash
python app.py
```

При первом запуске автоматически создастся файл `data/state.json` с демо-данными:
- 3 комнаты: Кухня, Спальня, Коридор
- Датчики: температура, влажность, движение, освещенность
- Устройства: свет, чайник, вентилятор, обогреватель
- 2 готовых правила автоматизации

## Использование

### Dashboard

Главный экран показывает:
- Карточки комнат с текущими показателями датчиков
- Быстрые кнопки: "Выключить весь свет", "Режим Ночь", "Я ушёл"
- Ленту последних событий

### Устройства

1. Нажмите "➕ Добавить устройство"
2. Выберите категорию (Датчик/Актуатор)
3. Выберите тип устройства
4. Укажите название и комнату
5. Для датчиков: настройте интервал обновления и режим генерации

### Автоматизация

Создание правила:
1. Нажмите "➕ Добавить правило"
2. Укажите условие (IF): выберите датчик, условие и значение
3. Опционально: установите временное окно
4. Укажите действие (THEN): выберите устройство и действие
5. Сохраните правило

Примеры правил:
- `IF температура > 26 THEN вентилятор ON`
- `IF движение triggered AND ночь (22:00-06:00) THEN свет в коридоре ON`

### Логи

- Просмотр всех событий системы
- Поиск по логам
- Экспорт в TXT файл

## Структура проекта

```
Smart-Home/
├── app.py                 # Точка входа
├── requirements.txt       # Зависимости
├── README.md             # Документация
├── data/
│   └── state.json        # Хранилище данных (создаётся автоматически)
├── src/
│   ├── core/             # Ядро приложения
│   │   ├── models.py     # Модели данных
│   │   ├── event_bus.py  # Система событий
│   │   ├── simulator.py # Симулятор устройств
│   │   └── automation.py # Движок автоматизации
│   ├── storage/          # Хранилище
│   │   └── storage.py    # JSON хранилище
│   ├── ui/               # Интерфейс
│   │   ├── main_window.py
│   │   ├── dashboard.py
│   │   ├── rooms.py
│   │   ├── devices.py
│   │   ├── automations.py
│   │   ├── logs.py
│   │   └── settings.py
│   └── utils/            # Утилиты
│       └── logger.py     # Логирование
```

## Технологии

- **Python 3.11+**
- **PySide6 (Qt)** - GUI фреймворк
- **JSON** - хранение данных

## Режимы работы

### Local Simulation Mode (по умолчанию)

Все устройства симулируются внутри приложения. Данные генерируются виртуальными "ESP32-узлами".

### MQTT Mode (опционально)

Режим для подключения к реальным MQTT брокерам (в разработке).

## Демонстрация на защите

### Что показать:

1. **Dashboard** - показать карточки комнат с обновляющимися данными датчиков
2. **Устройства** - добавить новое устройство, показать фильтрацию
3. **Автоматизация** - создать правило и показать его срабатывание в реальном времени
4. **Логи** - показать события и экспорт
5. **Быстрые действия** - продемонстрировать "Режим Ночь" или "Я ушёл"

### Ключевые моменты:

- ✅ Современный UI с тёмной темой
- ✅ Работающая автоматизация (правила реально срабатывают)
- ✅ Симуляция устройств в реальном времени
- ✅ Логирование всех событий
- ✅ Простое добавление устройств и правил

## Разработка

Проект структурирован с разделением на модули:
- `core/` - бизнес-логика
- `ui/` - интерфейс пользователя
- `storage/` - работа с данными
- `utils/` - вспомогательные функции

## Лицензия

Проект создан для дипломной работы.

## Автор

Данил
